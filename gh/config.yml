git_prootocol: https
editor:
prompt: enabled
pager:
aliases:
  clone: 'repo clone "$1"'
  search: 'api -X GET search/repositories -f q=''$1'' --template ''{{range .items}}{{ .full_name | color "white" }}: {{ .description }}{{"\n"}}{{ end }}'''
